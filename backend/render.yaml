services:
  - type: web
    name: my-api
    env: python
    plan: free
    buildCommand: |
      apt-get update && apt-get install -y alsa-utils libasound2 libasound2-dev
      pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port 8000
